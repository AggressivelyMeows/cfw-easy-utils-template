{
  "private": true,
  "name": "{{ project-name }}",
  "version": "1.0.0",
  "description": "A template for getting started using cfw-easy-utils",
  "main": "index.js",
  "scripts": {
    "publish": "wrangler publish",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "{{ authors }}",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "cfw-easy-utils": "^0.1.0"
  }
}
